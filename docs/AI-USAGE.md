# AI Usage Disclosure

The course explicitly requires honest disclosure of AI assistance (assignment §5.5).

Maintain a factual record of:
- tool used,
- date/phase,
- what the tool generated or shaped,
- what the students changed,
- why the changes were made.

Do not claim hand-written authorship for generated work.

Each contributor keeps a **separate table** below (so the two never edit the same lines and never conflict). Add a row in the same PR as the work it describes.

## Log — Contributor A (`TahaSohail-Goat`, Claude Code)

| Date | Phase | Tool / model | What it generated or shaped | What the students changed, and why |
|---|---|---|---|---|
| 2026-09-25 | 00 | Claude Code (Anthropic), model `claude-sonnet-5`, driven by the repository owner | Transcribed `docx/ASSIGNMENT_SOURCE.pdf` into `docx/ASSIGNMENT.md`; wrote `docx/EXTRACTION_NOTES.md`; extracted 304 requirement IDs into `docs/ASSIGNMENT_TRACEABILITY.md`; wrote `docs/RUBRIC.md`, `docs/SUBMISSION.md`, `docs/REPOSITORY_STRUCTURE.md`, `docs/ENVIRONMENT_PREREQUISITES.md`, `docs/BLOCKERS.md`, `docs/PHASE_STATUS.md`; created labels, issues, the `.gitignore`/`.gitattributes` files, issue templates and the directory skeleton; ran host tool detection. The governance prompt pack (`CLAUDE.md`, `AGENTS.md`, `PROMPT.md`, `docs/…` stubs, phase prompts) was supplied to the session as input. | The repository owner merged PR #9 on 2026-09-25 with no change to its content (merged tree identical to the branch tip); no written review exists. *Record here, with reasons, every correction the students make from now on.* |
| 2026-09-25 | 00 (follow-up #10) | Claude Code (Anthropic), model `claude-sonnet-5`, driven by the repository owner | Applied the owner's review decisions: retired the `dev/<n>-<slug>` convention, created the literal `dev` branch, rewrote the branch model / PR flow / merge-strategy rules in `docs/GITHUB_WORKFLOW.md`, and updated `PROMPT.md`, `AGENTS.md`, `docs/PHASE_EXECUTION_PROTOCOL.md`, `docs/CICD.md`, the PR template, `docs/BLOCKERS.md`, `docs/ASSIGNMENT_TRACEABILITY.md`, `docs/PHASE_STATUS.md`, `docs/TEAM_CONTRIBUTION.md`. | *Pending* — awaiting the partner's review of the PR. |
| 2026-09-25 | 00 (follow-up #10) / 01 setup | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` only | Tightened the `main` ruleset and created the `dev` ruleset (1 approval, no bypass, merge methods), disabled squash merge, exported the rulesets to `docs/evidence/`, wrote the two-account working agreement and review standard, created the Phase 01 issues #12–#18 with a 15/15 effort split. No action was taken as, or with the credentials of, `Artfever`. | *Pending* — both members to confirm the split and the settings in the PR thread. |
| 2026-09-25 | 00 (#59) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Drafted the questions for the instructor (issue #59); after the owner relayed the answers, recorded them in `docs/BLOCKERS.md` (answers table, removal of the resolved rows, new scope question B-022) and wrote the install plan per machine in `docs/ENVIRONMENT_PREREQUISITES.md`. Noticed from §5.1 that a two-week period matches the split "Assignment 1 = parts A–G" and raised it as B-022 instead of assuming; the owner then answered that the whole assignment A–J is due, and B-022 was moved to the answers table. | The instructor's answers were relayed verbally by the owner and are recorded as such (not in writing). No hand edits to these documents by the students are recorded. |
| 2026-09-25 | 00 (#59, review round) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Addressed the changes-requested review of PR #61: recorded Artfever's machine state in `docs/ENVIRONMENT_PREREQUISITES.md` §8 from the raw output his own session gave in the review (with a Windows note about `npm.ps1` and the execution policy), made the deferrals of B-008 and B-012 explicit, recorded the missing written AI-use statement as the open risk B-023, added his runtimes to B-020, and ticked or deferred the unchecked boxes of issue #59 with a reason for each. | The state of Artfever's machine is what his session reported; it was not re-run and nothing about his Windows version, RAM or WSL 2 was invented. The reviewer's findings and the owner's instruction were the input. No hand edits by the students are recorded. |
| 2026-09-25 | 01 (#13, PR #19) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote `docs/PRD.md` from `docx/ASSIGNMENT.md` (13 sections, a sequence diagram and a state diagram) and ran the checks on it (requirement IDs, blocker references, links, table lint, Mermaid parse). After Contributor B's review of the first version: linked `USE_CASES.md` from the workflow, functional-requirements and traceability sections and split section 3 into users, supporting systems and stakeholders (commit "link the use-case catalog and group actors by role"); drafted the reply that answers the reviewer's "why" question. | Owner decision applied: the nine endpoints listed in the assignment are the contract (B-004). No hand edits to this document by the students are recorded; all changes, including those after review, were made in this session. The two review findings were raised by Contributor B and applied as described. |
| 2026-09-25 | 01 (#15, PR #20) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote `docs/FRs.md` (catalog index, rules, entry template) and `docs/frs/api.md` (18 entries for `ASG-FR-020…037`, the status transition matrix, design questions), added B-021 to `docs/BLOCKERS.md`, and checked the catalog mechanically (entries and fields, matrix cells, design-question references, requirement IDs, links, tables). After Contributor B's review of the first version: removed statements the assignment does not fix — a success code and body and a 404 for `PATCH`, a `200` for `/health`, an `id` in the 201 body — and turned them into design questions DQ-API-05, -11, -17 and -18 (commits "state only what the assignment fixes for PATCH and /health" and "make FR-020 and FR-023 consistent about the complaint id"); drafted the reply that answers the reviewer's "why" question. | Owner decision applied: the nine endpoints listed in the assignment are the contract (B-004). The 18 design questions are deliberately left open for Phase 02. No hand edits to these documents by the students are recorded; all changes, including those after review, were made in this session. The review findings were raised by Contributor B and applied as described. |
| 2026-09-25 | 01 (#18) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Designed the allocation of Phases 02–12 (25 work packages, 70 points each), created the 36 GitHub issues (11 phase parents and 25 packages) with owner, reviewer, requirement IDs, acceptance criteria, file ownership and dependencies, filled Owner and Issue for 271 traceability rows, and wrote the allocation, totals, sequencing, file-ownership, planned-merge-conflict, demo-video and explain-back sections of `docs/TEAM_CONTRIBUTION.md` and the phase rows of `docs/PHASE_STATUS.md`. Checked the result with one-off scripts (not committed): points balance, ID-to-package mapping, issue links and assignees, table lint. | Owner decisions applied: keep the `dev` branch model (feature → dev → main); nine endpoints (B-004). The allocation is a **proposal** that both contributors must accept in the PR thread. No hand edits to these documents by the students are recorded. |
| 2026-09-25 | 01 (#18, owner decision) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Applied the owner's decision to keep the process light: moved #14, #16 and #17 from `Artfever` to `TahaSohail-Goat` in the Phase 01 allocation and recomputed the totals (100 / 70 points), replaced the one-phase-at-a-time sequencing with the actual dependencies, and rewrote `docs/PHASE_STATUS.md` to the current working agreement. | The reassignment and the lighter flow are the owner's decisions; the wording and the arithmetic are Claude Code's. No hand edits by the students are recorded. |
| 2026-09-25 | 01 (#16) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Generated `docs/NFRs.md` from the traceability matrix with a script (statements, sources and verification codes come from the matrix), wrote the category and measure of each row by hand, and checked that every number in the measures appears in `docx/ASSIGNMENT.md`. | The issue was first allocated to `Artfever` and moved to `TahaSohail-Goat` by the owner to save time; `Artfever` reviews it. No hand edits by the students are recorded. |
| 2026-09-25 | 01 (#17) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote the ten use cases in `docs/USE_CASES.md` from assignment §1.4, §2.1–§2.5, §3.2–§3.4 and the FR catalog, including the Mermaid sequence diagram for the triage fallback, and checked the diagram with the Mermaid parser and every ID against the matrix. | The issue was first allocated to `Artfever` and moved to `TahaSohail-Goat` by the owner to save time; `Artfever` reviews it. No hand edits by the students are recorded. |
| 2026-09-25 | 01 (#14) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote the frontend FR catalog `docs/frs/frontend.md` (17 entries in the shared entry template, a list of eight component-test scenarios for the five-test minimum, and ten design questions) from assignment §2.1, §3.4 and rubric B, and added its link to the FR index. | The issue was first allocated to `Artfever` and moved to `TahaSohail-Goat` by the owner to save time; `Artfever` reviews it. No hand edits by the students are recorded. |
| 2026-09-25 | 02 (#33) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Drafted `docs/API_DESIGN.md`: one error model, one list envelope, an answer to each of the 18 design questions marked as fixed by the assignment (with the clause) or as a design decision (with the reason), the shapes for the typed client, the `triaged_by` values and the seed command location; added an "Answered in" column to the design-question table in `docs/frs/api.md`. | The numeric defaults (page size 20, 10 requests per 60 seconds, 1 second dependency timeout) are design decisions for the reviewer to accept or change; the assignment gives none. No hand edits by the students are recorded. |
| 2026-09-25 | 04 (#37) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote the backend skeleton: settings without default connection strings, JSON logging on stdout with a request id, a request-context middleware (request id, access log line, metrics by route template), `/health`, `/ready`, `/metrics`, the shared error model, the readiness service with concurrent per-dependency timeouts, and 23 tests including one that shows an in-flight request completing after the shutdown signal. Ran ruff, mypy and the suite (94% coverage), and ran the real server against unreachable dependencies to check `/health`, `/ready` and `/metrics`. | Design choices such as the 1 second dependency timeout and hiding exception text from `/ready` follow `docs/API_DESIGN.md` and are Claude Code's; the real SIGTERM path could not be exercised on this Windows machine, so the test calls uvicorn's own signal handler. No hand edits by the students are recorded. |
| 2026-09-25 | 05 (#40) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote the Alembic environment and the first migration, the ORM models, the complaint repository (records, filters, paging, status update with an expected-status guard, counts, recent outcomes, idempotent bulk insert), the engine factory, `docs/DATA_MODEL.md` and 30 more tests. Downloaded a portable PostgreSQL 16.10 into a scratch directory to run the database tests locally, because Docker is not installed on this machine. | The database-level constraints beyond the assignment's own (the one-line `ai_summary`, the `triaged_by` pattern, non-negative latency) are Claude Code's design, for the reviewer to accept or change. No hand edits by the students are recorded. |
| 2026-09-25 | 04 (#38) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote the complaint API: Pydantic schemas, the four routes, the complaint service, the explicit transition table, the unit of work, the error handlers that replace FastAPI's 422 and `detail` bodies with the one error model, an in-memory repository and triage stub for tests, and 82 more tests (all 16 status pairs, every 400/404/409 case, a real-database round trip, and import-based layering checks). | The default page size (20), the 200-character limit on `reporter_contact` and the retry rule for a concurrent status change are Claude Code's design decisions for the reviewer to accept or change. No hand edits by the students are recorded. |
| 2026-09-25 | 07 (#44) | Claude Code (Anthropic), model `claude-sonnet-5`, in the session and GitHub account of `TahaSohail-Goat` | Wrote the triage layer: the `TriageResult` model and provider protocol with typed errors, `RuleBasedTriage` (keyword lists), the seeded `SimulatedTriage` with failure injection, the provider factory, and the orchestration (10 second cut-off, one retry with jitter on a timeout, 429 or 5xx, never on a 400, re-validation of every result, rule fallback with exactly one WARNING); the complaint service now generates the id before triage so the warning can name it. 67 more tests, including the mandatory always-raising-provider case over HTTP and on a real table. | The keyword lists, the confidence values of the rules and the 0.1 to 0.5 second jitter range are Claude Code's design, not fixed by the assignment; the reviewer should challenge them. No hand edits by the students are recorded. |

## Log — Contributor B (`Artfever`, Codex)

| Date | Phase | Tool / model | What it generated or shaped | What the student changed, and why |
|---|---|---|---|---|
| 2026-09-25 | 02 (#32) | OpenAI Codex (GPT-6), in Artfever's session and GitHub account | Drafted `docs/ARCHITECTURE.md` from assignment §2–§3 with component/network, layer, submit and probe diagrams; documented the hosted-LLM egress decision and failure paths; audited §5.7 paths against the tracked `dev` tree in `docs/REPOSITORY_STRUCTURE.md`. Used the Mermaid parser to check all five diagrams and `git ls-files` for the layout inventory. | Artfever directed the scope and file ownership; no manual edits by the student are recorded. The architecture labels source rules separately from design decisions so unassigned details are not presented as assignment requirements. |
