# Data Model

Use the assignment's minimum schema as the baseline.

Document:
- table/enum definitions
- constraints
- FK relationships
- indexes
- migration strategy
- seed strategy
- persistence demonstration
- backup/restore implications

Minimum complaint fields and domain values must match the assignment exactly unless an ADR documents a justified implementation representation that preserves the public contract.
