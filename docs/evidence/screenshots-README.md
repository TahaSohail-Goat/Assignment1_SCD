# README capture provenance — issue #55

Requirements: ASG-GEN-005..010, ASG-DOC-001..007; video requirements remain pending.

Captured on Artfever's Windows machine, 2026-09-26, from a **new clone** of `dev` at
`ba312b7efa3c11b177362b73ace015b208f0670e`, with new Compose networks and empty volumes.
Docker's existing image/build cache was available: this was a clean checkout/data test,
not a measurement of cold Internet downloads or a newly installed operating system.

## Compose procedure and results

1. `git clone --branch dev --single-branch https://github.com/TahaSohail-Goat/Assignment1_SCD.git D:\CivicPulse-ops\clean-clone-55`.
2. Run the README's first scriptblock invocation in that clone, unchanged. Environment
   overrides were `COMPOSE_PROJECT_NAME=civicpulse-readme55`, `FRONTEND_PORT=18080`,
   `BACKEND_PORT=18000` to isolate the existing local app. `.env` did not exist and was
   generated by the block. No credential value is captured.
3. `docker compose up -d --build --wait` completed with exit 0; database, cache, backend
   and frontend were healthy, and migration/seed exited 0. The initial console build output
   was not saved; the following raw PowerShell transcript records the verification commands
   and PowerShell HTTP responses:
   [screenshots-clean-clone-log.txt](screenshots-clean-clone-log.txt).
4. Readiness named PostgreSQL and Redis `ok`; list total was 30. Two stats requests returned
   MISS then HIT. Frontend `ping -c 1 database` failed with `bad address 'database'`.
   Reseeding printed `0 new complaints; 30 defined`. Native Docker output was visible in
   the tool console but omitted by PowerShell transcription in this environment. It was
   captured directly on a later repeat of the same status/log/isolation/reseed checks:
   [screenshots-compose-output.txt](screenshots-compose-output.txt). The initial migration
   log still records 30 inserts; the later repeat happens after the screenshot submission.

## Browser capture

Chromium through Playwright 1.62.1, viewport 1280×1000, real HTTP requests, no mocks,
on `http://localhost:18080`. The computer-use browser tool could not initialize, so the
installed Playwright browser was used. No application code was changed for capture.

- Entered the synthetic complaint visible in `screenshots-submit.png` and clicked Submit.
  POST returned **201**, provider **rules**, id `7f9e15fe-099e-4044-9dcc-52432a3c721b`.
  A following GET returned **200** and matching text. The Submit capture is full-page.
- Dashboard displayed that complaint and **31 complaints**. Its screenshot is the first
  viewport of the actual page, keeping the text readable instead of shrinking a long page.
- Opened Stats, switched away and returned within its cache TTL; the captured header is
  a real **HIT**, with total **31** and water count **6**.
- Browser emitted **zero page errors**. All three PNGs were visually inspected.

These screenshots demonstrate rules classification and live storage/cache/UI integration.
They do **not** demonstrate a live hosted LLM, Ollama or fallback, nor replace the required
video with both partners speaking.

## Kubernetes procedure and results

The README Kubernetes block was extracted directly from its fenced code and executed by
Windows PowerShell 5.1. The first attempt created a new `kind-civicpulse-demo` cluster,
installed prerequisites, loaded images and reached healthy workloads, but its helper
function consumed the unquoted `--` in the seed call. Preserved output:
[first attempt](screenshots-kubernetes-log.txt).

Quoting `'--'` fixed seeding (30 inserted). The second attempt then exposed PowerShell's
comma-list expansion in the helper's final `get pods,hpa,vpa`; preserved output:
[second attempt](screenshots-kubernetes-retry-log.txt). Quoting `'pods,hpa,vpa'` fixed it.

The entire corrected block was executed again, unchanged, against the cluster created by
attempt one. It completed with exit **0**, reseeded **0** duplicates, and listed all six
workload pods Ready, HPA **2 replicas / CPU 1% of 60% target**, and VPA **Off** with a
recommendation. The [PowerShell transcript](screenshots-kubernetes-final-log.txt) omitted
native stdout, so the exact corrected block was run once more under a subprocess that
captured stdout/stderr directly: [complete native output](screenshots-kubernetes-output.txt).
These final invocations reuse the cluster; neither is a second fresh-cluster experiment.

The exact documented Ingress port-forward and Host-header requests returned frontend
**200** and **30** seeded complaints in stats:
[Ingress smoke transcript](screenshots-kubernetes-smoke-log.txt).
The local images came from the clean-clone Compose build above; published-digest deployment
is separately evidenced by CD run 36230267941. Neither local test is a new HPA load experiment.

All nine method/path pairs in the README matched the running backend's `/openapi.json`.
All relative README links resolved to existing files. The Mermaid diagram was inspected
against Compose and the architecture document; no automated Mermaid rendering was run.

## Submission audit observations

`python scripts/check_submission.py`: **25 pass, 1 warning, 2 fail** on this branch before
commit. LICENSE is genuinely missing. The scaling-chart failure is a filename heuristic:
the existing measured plot is `k8s-load-comparison.png` and its method is in
`k8s-load-README.md`; the script searches for `chart` in the filename. The historical
non-merge warning needs the final audit's PR-history explanation.

The script's contribution check only confirms both authors exist and searches all refs.
The team's documented command on `origin/main` at `8074879`,
`git shortlog -sn --no-merges origin/main`, gives Taha 80 and Artfever 25 (**23.81%**).
Including merges gives Taha 97, Artfever 32 (**24.81%**). Neither satisfies the 35% minimum.
Reassess on the final branch and allocate genuine outstanding work; do not pad commits.
