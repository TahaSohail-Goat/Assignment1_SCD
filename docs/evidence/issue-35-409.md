# Issue #35: forbidden status transition in the Dashboard

The screenshot [`issue-35-409.png`](issue-35-409.png) was captured on 2026-09-26 from the actual Vite/React frontend in headless Chrome. A Playwright route supplied a controlled API response: the complaint list returned one `open` complaint, and selecting `resolved` sent a `PATCH` that returned HTTP 409 with `error.message` equal to `Cannot transition from open to resolved.` The screenshot shows that exact server message beneath the complaint. The client did not infer or display its own transition rule.

Command: `python capture_409.py` (temporary capture script, removed after the run). Output: `Observed status: 409; displayed message: Cannot transition from open to resolved.` The request was mocked at the browser network boundary; this capture does not claim a live backend interaction.

Requirements: ASG-FR-009, ASG-FR-010; issue #35.
