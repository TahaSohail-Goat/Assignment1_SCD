# START HERE

1. The supplied assignment source is already included as `docx/ASSIGNMENT_SOURCE.pdf`.
   - If a DOCX version is supplied later, also keep `docx/ASSIGNMENT_SOURCE.docx` unchanged.
2. Start Claude Code in the repository root.
3. Tell Claude Code to read `CLAUDE.md`, `AGENTS.md`, and `PROMPT.md`.
4. Run only Phase 00 first.
5. After Phase 00, proceed one phase at a time.
6. Do not merge documentation PRs until the repository review is complete.
7. Enable main branch protection before implementation PRs are merged.

Important:
- This pack contains prompts and governance artifacts.
- It does not pretend to have implemented the assignment.
- Claude Code must build the actual project from the authoritative assignment source.
